apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "launcher.fullname" . }}-test-connection"
  labels:
    {{- include "launcher.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "launcher.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
