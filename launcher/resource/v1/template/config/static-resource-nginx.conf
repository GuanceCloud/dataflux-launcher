server {
    listen 80;
    server_name {{ domain.subDomain.staticResource }}.{{ domain.domain }};
        location / {
        root   /usr/share/nginx/image/staticFolder;
        index  index.html index.htm;
    }
}