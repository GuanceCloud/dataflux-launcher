server {
        listen 80;
        server_name {{ other.domain }}, www.{{ other.domain }};

        location / {
           root /config/cloudcare-forethought-webclient;
           index index.html;
           try_files $uri $uri/ /index.html;
        }
}
